import DefaultLayout from '../../layout/DefaultLayout';
import Breadcrumb from '../../components/Breadcrumbs/Breadcrumb';

const PendingDeposits = () => {
  return (
    <DefaultLayout>
      <Breadcrumb pageName="Pending Deposits" />
    </DefaultLayout>
  );
};

export default PendingDeposits;